# Coverage-delta gate ack file (issue #415, incident #389).
#
# .github/scripts/coverage_delta.py fails a PR when a Playwright test title
# present at the merge-base is either absent at head or went from active to
# skipped, UNLESS this file carries a matching line. Same tether-discipline
# shape as docs_lint.py's in-file ALLOWLIST: mechanical check, human-written
# escape hatch, one line per exception, reason required.
#
# Format (append, never edit/remove someone else's line without checking
# with them first - these are a permanent record of a deliberate call, not
# a scratchpad):
#
#   coverage-ack: <test id or glob> — <reason>
#
# <test id> is `<path/to/File.spec.ts>::<full title>` (nested
# test.describe() titles joined with " > ", matching coverage_delta.py's
# own manifest key). A glob (`*`, `?`, `[...]`) is fine for "this whole
# file" or "this whole describe block" - e.g.:
#
#   coverage-ack: frontend/tests/Foo.spec.ts::* — reason
#   coverage-ack: frontend/tests/Foo.spec.ts::some describe > * — reason
#
# Lines starting with `#` and blank lines are ignored.

# Proposal H parked-spec port wave (2026-07-24, issue #272) - owner-ruled drops (file deleted,
# not just re-skipped). Full parity/upstreamability tables live in this wave's own PR body, not
# duplicated here - one line per exception, per this file's own convention above.

# Owner-ruled DROP: the aria-snapshot coverage of the classic grid's per-slot common-cardback
# swatch has no unified-page replacement to port onto (CommonCardback.tsx's own editor mount is
# classic-/editor-only, fully unrouted since the Proposal H swap) - the owner's replacement UX
# (an export-time reminder) is a separate, not-yet-built design, tracked apart from this wave.
coverage-ack: frontend/tests/visual/CommonCardback.visual.spec.ts::* — owner-ruled DROP, replacement UX is a separate not-yet-built design (see PR body)

# Owner-ruled DROP: all 12 tests cover bulk multi-select editing (select N slots, then bulk
# change-query/change-image/delete/select-similar/select-all) - a real, tracked capability gap on
# the unified /editor page (issue #272 item 6, no bulk-select surface exists there at all today),
# not a redundant or obsolete test. Same finding CardSlot.spec.ts's own wave-3 port comment
# already documents independently.
coverage-ack: frontend/tests/SelectedImagesRibbon.spec.ts::* — owner-ruled DROP, bulk multi-select has no unified-page equivalent yet (tracked gap, issue #272 item 6 - see PR body)

# Owner-ruled DROP: all 4 tests assert CSS/testids (left-panel/right-panel/add-cards-panel,
# OverflowCol's chainScroll overscroll-behavior-y) that only exist on the classic, fully-unrouted
# grid layout. The unified page's own mobile fix (issue #266) is a bottom-sheet drawer, not
# stacked-panel scroll chaining, and already has its own dedicated coverage in DisplayPage.spec.ts's
# "phone viewport (issue #266)" describe block.
coverage-ack: frontend/tests/ProjectEditorMobileScroll.spec.ts::* — owner-ruled DROP, subject CSS/testids only exist on the classic unrouted layout; #266's own mobile fix has separate coverage (see PR body)

# Un-skip + re-home port of PostExportContributionPrompt.spec.ts (issue #166) onto /print's own
# PDFGenerator mount - renamed off its stale "classic Print! tab" title (the classic grid is
# fully unrouted; PDF generation lives solely on /print now) in the same change, same convention
# PR #337 established for this file's title staleness pattern. The new title carries the
# unchanged behavior forward (un-skipped, not removed) at a new, non-stale title.
coverage-ack: frontend/tests/PostExportContributionPrompt.spec.ts::Post-export contribution prompt (issue #166) - classic Print! tab > also appears after a successful export from PDFGenerator.tsx's own classic tab — renamed + un-skipped in the same change, see the file's own new title

# Cardback flow round (SPEC-cardback-pdfwait.md §C.2) - the toolbar cardback picker no longer
# auto-closes on select (the apply-all/set-default prompt renders inline instead), so these 3
# titles changed to describe the new stays-open behaviour; the underlying selection coverage is
# preserved, not dropped - see this PR's own body for the full before/after.
coverage-ack: frontend/tests/GridSelectorModal.spec.ts::GridSelectorModal – JumpToVersion > submitting a valid option number selects that card and closes the modal — cardback flow round (SPEC-cardback-pdfwait.md §C.2): the toolbar cardback picker no longer auto-closes on select (the apply-all/set-default prompt renders inline instead), so the title changed to describe the new behaviour; the underlying selection coverage is preserved, not dropped.
coverage-ack: frontend/tests/GridSelectorModal.spec.ts::GridSelectorModal – JumpToVersion > submitting a valid identifier selects that card and closes the modal — same rename as the sibling "option number" test above, same reason.
coverage-ack: frontend/tests/GridSelectorModalVariants.spec.ts::GridSelectorModal - keyboard navigation > Tab reaches a result card and Enter selects it, same as a click would — cardback flow round (SPEC-cardback-pdfwait.md §C.2): same auto-close-removed rename as GridSelectorModal.spec.ts's JumpToVersion tests.

# WTC rebuild (2026-07-24, SPEC-wtc-rebuild.md, PR #446) - /whatsthat's visual/layout tree was
# rebuilt from scratch (container-first hero, Tokyo-11 tokens, owner rulings on BurstSvg/
# session-counter/WD3 phone compaction). Every ack below either (a) retires a test guarding a
# mechanism the rebuild deliberately deletes (sticky/bounded-height pinning, BurstSvg/HoverBurst,
# the gold/navy button treatment, the whatsthat-mark.svg glyph asset, the sliced-word pop
# animation) - not a coverage regression, since the underlying behavior no longer exists to test
# - or (b) covers a describe-block rename where the test's own assertions were kept/adapted, not
# dropped, under a new (still-covered, see the sibling test file) title. Full reasoning + the
# spec-section mapping live in PR #446's own body, not duplicated here.
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - Level 2 layout reconciliation (real-device regression guard) > * — describe renamed to "Level 2 layout containment" (byte-identical containment/overlap assertions kept, no sticky/burst mechanism left to describe as "reconciliation") - PR #446
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - mobile layout > * — WD4 retires the 100dvh-bounded hero + its own internal overflow-y:auto questions column entirely (ordinary scrolling document now); the axis-flip/mobile-top-edge invariants are kept, re-pointed at the new container-first DOM under a new "container-first hero layout" describe; the two scroll-pinning tests and the 1400x900 no-internal-scroll test are dropped outright (no bounded height left to assert against) - PR #446
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - mobile card/questions never overlap (owner live-review fix) > * — guarded a position:sticky overlap bug that can't recur once nothing on this page is position:sticky (WD4); the container-first fold (flex-wrap) structurally prevents the two columns from ever sharing a stacking position instead - PR #446
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - hover-zoom/hover-burst edge clipping (owner live report) > * — owner ruling 1: BurstSvg/HoverBurst retired outright, nothing left to clip - PR #446
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - portrait static top block (owner live-review) > * — WD4 retires the "everything above the fold, only the candidate row scrolls" invariant entirely; the page scrolls normally now - replaced by WD3-specific subject-compaction coverage under a new describe, not a same-shaped port - PR #446
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - question-mark motif + golden action buttons (owner review round 2) > * — WD1: retires the whatsthat-mark.svg gold-gradient glyph asset and the QUIZ_BUTTON_GOLD/_NAVY treatment; replaced by token-based equivalents under a new describe - PR #446
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - shared blue mystery card composition (owner review round 3) > * — the shared MysteryCard component is retinted onto tokens and its glyph becomes plain text (WD1); replacement coverage lives under the new "mystery-card glyph" describe - PR #446
coverage-ack: frontend/tests/WhatsThatWordsAnimation.spec.ts::What's That Card? - sliced-word pop + hero card pulse (issue #305) > * — retires the sliced WHAT'S/THAT/CARD? pop sequence and the card-pulse-in-sync-with-THAT effect entirely (WD1 kills the gold/navy identity these animations were built around; ANNEX C's animation inventory doesn't list either) - the file is re-purposed for ANNEX C's actual reduced-motion coverage, not a like-for-like port - PR #446
coverage-ack: frontend/tests/WhatsThatWordsAnimation.spec.ts::What's That Card? - narrow-width wordmark swap (owner live-review) > * — retires the WideWordmark/NarrowWordmark viewport-driven CSS-display fork (container-first policy); WhatsThatWords is now one static, single-tree <h1> - PR #446

# WTC phase B (PR #528) - NoMatchReasonStrip's seven reason chips were split into two
# labelled groups (not-official-printing / not-official-art). Both titles below picked up
# wording describing that split in the same change; neither test was removed - both are
# still present, still exercise the same strip, and the file gained two further tests on top
# (the exhaustive/no-overlap partition check, and a cross-group vote-payload-shape check),
# taking this file from 4 tests to 6.
coverage-ack: frontend/tests/NoMatchReasonStrip.spec.ts::NoMatchReasonStrip tests > No match is disabled until a chip is set, then shows the reason strip (not the general attribute panel) — renamed to "No match is disabled until a chip is set, then shows the reason strip split into two labelled groups (not the general attribute panel)" when the strip was split into two axes; test still present and extended with per-group assertions.
coverage-ack: frontend/tests/NoMatchReasonStrip.spec.ts::NoMatchReasonStrip tests > hides chips for reason tags that don't exist server-side yet — renamed to "hides chips for reason tags that don't exist server-side yet, in both groups" when the strip was split into two axes; test still present and extended to assert the unseeded-tag filter applies inside a still-rendered group, not just across whole groups.

# WTC phase C2 (issue #503, PR #551) - phase C1 was deliberately presentation-only, so this
# test asserted that tapping any candidate, grouped or ungrouped, submitted the IDENTICAL
# /2/submitPrintingTag/ payload. C2 intentionally supersedes that contract: a grouped tap now
# submits ONE illustrationId to the new /2/submitIllustrationVote/ endpoint instead, while an
# ungrouped tap stays on /2/submitPrintingTag/ unchanged. The old title now asserts the
# opposite of what's true, so it was renamed (not deleted) and split into two tests - one per
# branch - describing the new split; see the file's own "question feed - Level 2 illustration
# grouping" describe block for both replacements.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - Level 2 illustration grouping > selecting a grouped candidate submits the identical payload to the identical endpoint as an ungrouped one — superseded by C2 (#503/PR #551): renamed + split into "selecting a grouped candidate submits its illustrationId to /2/submitIllustrationVote/, not a printing list" and "selecting an ungrouped candidate (distinct or null illustrationId) still submits to /2/submitPrintingTag/, unchanged" - the old title asserted C1's now-superseded sameness contract, not a coverage drop.

# MTGAC artist-links M2 applet round (docs/features/artist-support-links.md) - ArtistSupportLink
# became a self-contained applet (RTK Query fetch, commerce-link buttons, Mark's Signature
# Service badge, MTGAC page link, MTGAC credit line) rather than a single caller-styled `<a>`
# wrapping caller-supplied children/className. The rail's page-link button now names the artist
# (was "Support on MTG Artist Connection") and reads as a solid btn-primary button (was the
# caller-supplied btn-outline-primary) - the destination-naming credit moved into the applet's
# own "Source: MTG Artist Connection" line, asserted separately in the renamed test below. Test
# still present, still covers the same surface, extended with the new credit-line assertion.
coverage-ack: frontend/tests/DisplayPage.spec.ts::DisplayPage (Proposal H, Step 1) > the promoted artist line shows a plain credit + a support button naming MTG Artist Connection for a card with a known canonical artist (E2 - promoted, no longer a collapsed accordion; SPEC-display-left-rail.md §5/§8) — renamed to "the promoted artist line shows a plain credit + the MTG Artist Connection applet (page-link button, credit line) for a card with a known canonical artist (M2 applet round, docs/features/artist-support-links.md)" when ArtistSupportLink became a self-contained applet; the button now names the artist instead of the destination (the applet's own credit line names the destination instead, newly asserted), and its class changed from caller-supplied btn-outline-primary to the applet's own btn-primary. Test still present, same surface, extended not dropped.

# Issue #301 (croppable bleed, merged bleed boxes, split-the-difference gutters) - layout.ts's
# fit math changed from "full bleed or drop a card" to "bare-card-count-first, bleed crops to
# whatever the space affords" (see layout.ts's own module comment / fitAxisWithBleed). The old
# title asserted the very behavior #301 was written to remove (a bordered profile dropping the
# sheet from 4x2 to 3x2 once bleed exceeded the cap) - that claim is now false, not merely
# renamed cosmetically, so the test was rewritten to assert the new (intended) outcome: the
# sheet stays a full 4x2, bleed on the affected edge crops to the cap instead. Same surface
# (Page Setup margin-profile control + live sheet slot count), same cap arithmetic
# (marginProfiles.test.ts's maxBleedForFourColumns is unchanged by #301 - only what exceeding
# it DOES changed), test still present and extended with a disclaimer-text assertion.
coverage-ack: frontend/tests/DisplayPage.spec.ts::DisplayPage (Proposal H, Step 1) > the margin-profile control defaults to Borderless (no warning) and switching to Bordered shrinks the sheet from 4x2 to 3x2, with a cap warning shown — renamed to "the margin-profile control defaults to Borderless (no warning) and switching to Bordered crops bleed rather than dropping to 3x2, with a cap warning shown (#301)" when #301 replaced the old "drop a column" behavior with per-edge bleed cropping; the sheet no longer shrinks to 3x2 at all under the new fit math, which is the fix this issue shipped, not a regression.

# Proposal F /stats transform (PR #558, 2026-07-29) - /contributions became a plain client-side
# redirect shell to /stats; the Contribution Guidelines accordion these two tests audit still
# exists verbatim (features/stats/ContributionGuidelines.tsx), it just renders on /stats now.
# Same surface, same assertions, only the URL (and the describe/test title naming it) changed.
coverage-ack: frontend/tests/ContrastAudit.spec.ts::Contrast audit - /contributions (owner defect 1+2: accordion header + body) > Contribution Guidelines accordion - collapsed and expanded — route moved /contributions -> /stats in PR #558; same surface, same assertions
coverage-ack: frontend/tests/ContrastAudit.spec.ts::Link colour audit (owner-approved open item 2 - $link-color -> $theme-info) > a real production link (contributions guidelines' ISO-639-1 reference, inside the accordion body's panel-bg) clears strict-AAA-normal — route moved /contributions -> /stats in PR #558; same surface, same assertions

# PR #730 - QuestionFeed.tsx's illustration-group render now emits ONE representative tile per
# group (`group.find((c) => c.artCropUrl) ?? group[0]`) instead of one tile per member, so a
# non-representative member (candidateB in this fixture set) no longer gets its own DOM tile
# anywhere - it's represented by the group's shared vote, not silently dropped. The three old
# titles asserted the retired one-tile-per-member behavior; all three are renamed (not deleted)
# to describe the new representative-tile contract, same surface, same fixture set.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - Level 2 illustration grouping > every candidate in a mixed illustration set still renders in the grid - none silently dropped — PR #730: renamed to "every candidate in a mixed illustration set is accounted for - a group collapses to its one representative tile, ungrouped candidates still render individually" now that a group renders one representative tile, not one tile per member.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - Level 2 illustration grouping > candidates sharing an illustration render inside one illustration-group container; unique/null-illustration candidates don't — PR #730: renamed to "only the illustration group's representative candidate renders inside its one illustration-group container; unique/null-illustration candidates don't", same reason as the sibling ack above.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - Level 2 illustration grouping > clustered tiles render each candidate's art crop, falling back to the printing scan when absent; ungrouped tiles keep the printing scan regardless — PR #730: renamed to "the illustration group's one tile renders its representative candidate's art crop; ungrouped tiles keep the printing scan regardless", same reason as the sibling acks above.

# PR #731 (issue #712) - Level 1 "Not sure" now fires a fire-and-forget POST to the new
# /2/submitQuestionAbstention/ endpoint before transitioning to Level 2, in addition to the
# unchanged "no printing vote cast" behavior this test already covered. Renamed (not deleted) to
# describe both halves of the contract, and extended with a mocked handler + payload assertion
# for the new POST - same surface (the NOT SURE button), same "no printing vote" claim kept.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > NOT SURE drops to Level 2's candidate grid without casting a vote — PR #731 (issue #712): renamed to "NOT SURE drops to Level 2's candidate grid without casting a printing vote, but does POST an abstention" now that NOT SURE fires a fire-and-forget POST to the new /2/submitQuestionAbstention/ endpoint; the "no printing vote" assertion is kept, extended with coverage of the new abstention POST.

# Layout pass (2026-08-06, issues #705/#707/#710/#711) - A4 amendment (SPEC-wtc-rebuild.md,
# 2026-08-05 owner ruling) supersedes the attribute-chip panel's collapsed-by-default behavior:
# it's now shown automatically for identify_printing/confirm_suggestion questions. The old
# title's factual claim ("collapsed by default") is false under the new default, so it was
# renamed rather than left stale; the underlying toggle-still-works coverage is kept, not
# dropped - see the new title's own body.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - Level 2 (candidate grid) > the attribute-chip filter is collapsed by default and expands on tap — renamed to "the attribute-chip filter is shown automatically for identify_printing questions, and can be hidden" (A4 amendment, SPEC-wtc-rebuild.md): the panel now defaults to shown for this question type instead of collapsed; the toggle-still-works assertion is kept, just inverted to match the new default.

# Layout pass (2026-08-06, issue #707) - AttributeChipPanel's ring-around-card composition
# (CardArea, used only by this page's Level 2 call site) is retired: the chip panel now renders
# in QPanel, never sharing a box with the reference card, so the "ring squeezes the card at a
# narrow width" failure mode this test guarded against can no longer occur structurally. Dropped
# in favour of a direct "chips never overlap the pinned card" check under a new title, covering
# the same underlying hard constraint (chips must never occlude the reference card) against the
# new mechanism instead of the retired one.
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - Level 2 layout containment (real-device regression guard) > at 360px with the attribute-chip filter expanded, the ring collapses to a stack instead of squeezing the card — dropped: the ring-around-card composition (CardArea) it guarded is retired by issue #707 (chips render in QPanel now, never sharing a box with the card); replaced by "at 360px with the attribute-chip filter shown, the chips never overlap the pinned reference card" covering the same never-occlude constraint against the new mechanism.
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - tap target sizes (mobile funnel pass) > Level 1's stacked answer buttons meet the 44px floor — the fixed Level 1/2/3 ladder was removed by the #728 de-hardcoding; this flow no longer exists
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - hover-zoom is not clipped by its frame (issue #705) > the Level 1 reference thumbnail's frame stops clipping while hovered — the fixed Level 1/2/3 ladder was removed by the #728 de-hardcoding; this flow no longer exists
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - Level 1 answer-row hierarchy (issue #711) > the Yes button sizes to its content instead of spanning full width (issue #740) — the fixed Level 1/2/3 ladder was removed by the #728 de-hardcoding; this flow no longer exists
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - Level 1 answer-row hierarchy (issue #711) > the Yes button reads at the same font size as its 'Not sure' sibling — the fixed Level 1/2/3 ladder was removed by the #728 de-hardcoding; this flow no longer exists
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > lands on Level 1 - a single suggested printing, no grid - and shows the 'Is it this one?' prompt — the fixed Level 1/2/3 ladder was removed by the #728 de-hardcoding; suggestion and grid now render on a single page
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > YES confirms the suggested printing directly, without visiting the grid — the fixed Level 1/2/3 ladder was removed by the #728 de-hardcoding; the grid now coexists with the suggestion on the same page
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > NOT SURE drops to Level 2's candidate grid without casting a printing vote, but does POST an abstention — the fixed Level 1/2/3 ladder was removed by the #728 de-hardcoding; the grid now coexists with the suggestion on the same page
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > NO drops to Level 2's candidate grid, excluding the rejected suggestion, without casting a vote — the fixed Level 1/2/3 ladder was removed by the #728 de-hardcoding; the grid now coexists with the suggestion on the same page
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > NO on the suggestion collapses its slot (never a selectable tile again) and keeps the remaining candidates selectable on the same page, without casting a vote — restructured in WTC design intent repass (#704); slot collapse behavior updated
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > NO on a singleton suggestion (no other candidates) skips the grid entirely and immediately casts the terminal no-match vote — restructured in WTC design intent repass (#704); slot collapse behavior updated

# WTC-SPEC.md composition/answer-set pass (2026-08-11, PR #770) - confirm_suggestion's answer
# set changed to Yes / "Same art, but..." / "Not this art" / Skip (SPEC §6/§7.1, rulings
# ledger #1-#2); "NO" and "NOT SURE" are retired names, not retired behavior. The >= 2
# illustration-cluster rule is also deleted (SPEC §7.2/§9): a distinct-illustrationId
# candidate with no sibling now forms its own singleton illustration group and casts an
# illustration vote instead of a printing vote, where it previously stayed ungrouped. Each
# line below states which of those two changes drove the rename/split, and names the still-
# present replacement test asserting the same or (where SPEC explicitly changed the contract)
# the new intended behavior.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - Level 2 illustration grouping > only the illustration group's representative candidate renders inside its one illustration-group container; unique/null-illustration candidates don't — SPEC §7.2/§9: the >= 2 cluster rule is deleted, so a distinct/unique-illustrationId candidate (previously excluded from clustering) now forms its own singleton illustration group instead of staying ungrouped. Renamed to "only the illustration group's representative candidate renders inside its own illustration-group container; a null-illustration candidate never clusters" - test still present, now asserting the two-group (shared cluster + singleton cluster) contract instead of the retired unique-stays-ungrouped one.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - Level 2 illustration grouping > selecting an ungrouped candidate (distinct or null illustrationId) still submits to /2/submitPrintingTag/, unchanged — SPEC §7.2/§9: singletons now cast illustration votes, not printing votes, so the old title's "distinct illustrationId" half is now false, not merely renamed. Split into two tests: "selecting a null-illustration candidate still submits to /2/submitPrintingTag/, unchanged" (null-illustration case kept, unchanged) and "a singleton illustration still submits its illustrationId to /2/submitIllustrationVote/, never /2/submitPrintingTag/" (distinct/singleton case now goes the opposite way, per the cluster-rule deletion).
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > NO on a singleton suggestion (no other candidates) casts the terminal no-match vote immediately, keeping the rejected candidate as a de-emphasised grid tile — SPEC §7.1 rulings ledger #1: "NO" is retired; its replacement "Not this art" routes to identification rather than auto-casting a vote (the auto-cast-on-singleton shortcut belonged specifically to the retired "no other candidate matches" claim, which "Not this art" doesn't make). Renamed and behavior-changed to "'Not this art' on a singleton suggestion leaves the rejected candidate as the grid's one de-emphasised tile and casts nothing automatically" - the user now reaches "None of these" via one explicit tap, same as identify_printing.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > NO on the suggestion collapses its slot (rejected candidate stays in the grid as a de-emphasised, re-selectable tile) and keeps the remaining candidates selectable on the same page, without casting a vote — pure rename, SPEC §7.1: "NO" -> "Not this art". Renamed to "'Not this art' gives way to the candidate-grid identification question, where the rejected suggestion stays as a de-emphasised, re-selectable tile, and keeps the remaining candidates selectable, without casting a vote" - identical assertions (slot collapse, de-emphasised re-selectable tile, remaining candidates selectable, no vote cast), same test, new answer name only.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > NOT SURE records an abstention and advances to the next question, without casting a printing vote — SPEC §6/§7.1: the answer set is Yes / "Same art, but..." / "Not this art" / Skip - there is no "NOT SURE" ("Yes and Skip are present on every confirmation", §6). Renamed to "SKIP records an abstention and advances to the next question, without casting a printing vote" - identical abstention-and-advance assertions, new answer name only.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - confirm_suggestion question type > lands on the suggested-match question - the suggestion is asked about in its own slot on page one, never re-judged as a grid tile — pure rename dropping stale Level 1/2/3 ladder language (SPEC §5/§9: no fixed sequence, "the page composes per question"). Renamed to "lands on the suggested-match question - the suggestion is asked about in its own slot, never re-presented as a grid tile" - identical assertions.
coverage-ack: frontend/tests/QuestionFeedResponsive.spec.ts::question feed - suggestion-slot answer-row hierarchy (issue #711) > the Yes button reads at the same font size as its 'Not sure' sibling — DESIGN-REPASS Rule 1 / issue #711 (uniform button geometry) is still in force; SPEC §0 folds DESIGN-REPASS's six rules in whole. Renamed to "the Yes button reads at the same font size as its 'Same art, but...' sibling" because the "Not sure" comparison target no longer exists in the new answer set (§7.1) - the same font-size-uniformity assertion is re-pointed at the new sibling button instead of being dropped.

# Granted-vs-requested bleed readout (issue #301 follow-up) - the margin-profile control's
# default changed from Borderless to Rear-feed, and the boolean cap warning (display-margin-
# profile-note's "⚠" / "cropped to" text) was replaced outright by BleedGrantedReadout, which
# states the actual granted bleed value per axis instead of a yes/no flag. Neither the old
# default nor the old warning element exists any more, so the old title's claims are false, not
# cosmetically stale. The replacement test "the margin-profile control defaults to Rear-feed,
# the readout states the granted (not just requested) bleed, and switching to Bordered still
# renders a full 4x2 with a different granted number" in the same file covers the same surface
# (default profile value, cap-crossing behavior on switching to Bordered, full 4x2 slot count
# preserved) against the new mechanism; BleedGrantedReadout.test.tsx and MarginProfileControl.
# test.tsx cover the granted-bleed/cropped-flag and default-profile behavior at the unit level.
coverage-ack: frontend/tests/DisplayPage.spec.ts::DisplayPage (Proposal H, Step 1) > the margin-profile control defaults to Borderless (no warning) and switching to Bordered crops bleed rather than dropping to 3x2, with a cap warning shown (#301) — the test asserted a default margin profile of Borderless and a boolean cap warning that neither exist any more: the default is now Rear-feed and the warning was replaced by BleedGrantedReadout's granted-bleed-value display. Covered by "the margin-profile control defaults to Rear-feed, the readout states the granted (not just requested) bleed, and switching to Bordered still renders a full 4x2 with a different granted number" in the same file, plus BleedGrantedReadout.test.tsx and MarginProfileControl.test.tsx at the unit level.

# Editor export rescue (docs/features/pdf-generator.md's "Page cut guide lines, Google Drive
# save, and retiring the Finish footer's own print route") - the Finish footer's separate
# co-equal "Print / Export ->" button (a client-side navigation to /print) is gone; PDF export
# now runs in place from the editor's own Export dropdown (DisplayExportPDF.tsx). Every removed
# test below drove that button, and every one has a same-file, same-describe-block renamed test
# that drives the same behaviour through the Export dropdown instead and asserts a completed
# download in place of the old navigation - none of these are coverage gaps.
coverage-ack: frontend/tests/CardbackFlow.spec.ts::Cardback reminder gate (SPEC-cardback-pdfwait.md §C.1, PKG1a) > appears for a project still riding the default cardback; dismissing it (✕) still proceeds (OWNER AMENDMENT 1), and a second attempt this session is silent (CB1) — the test reached the cardback reminder gate by clicking the removed Print/Export button. The gate itself, OWNER AMENDMENT 1 (dismiss-still-proceeds), and CB1 (once-per-session) are covered by the same describe block's renamed test "appears for a project still riding the default cardback; dismissing it (✕) still proceeds with the export (OWNER AMENDMENT 1), and a second attempt this session is silent (CB1)", which reaches the same gate via Export dropdown -> PDF -> Download and asserts a completed download instead of a navigation to /print.
coverage-ack: frontend/tests/DisplayFinishFooter.spec.ts::/display Finish footer (issue #275) > anonymous: shows a sign-in link in place of Save Deck, and Print / Export navigates straight to the Print page — the Print/Export button this test drove no longer exists. The anonymous, no-save-gate, cardback-gate-then-export path is covered by the same file's renamed test "anonymous: shows a sign-in link in place of Save Deck, no Print/Export button, and PDF export runs in place after the cardback gate (no save gate)", which additionally asserts the old button is gone and the export completes as a download in place, with no navigation.
coverage-ack: frontend/tests/DisplayFinishFooter.spec.ts::/display Finish footer (issue #275) > authenticated + dirty: Print / Export shows the save gate; choosing Save unlocks, saves, and lands on the Print page — same removed Print/Export button. The save-gate-then-persist sequence (unlock, save, and only then proceed) is covered by the same file's renamed test "authenticated + dirty: PDF export shows the save gate; choosing Save unlocks, saves, and the export still runs", which asserts the download completes only after the save request resolves, in place of the old navigation to /print.
coverage-ack: frontend/tests/DisplayFinishFooter.spec.ts::/display Finish footer (issue #275) > authenticated + dirty: Skip on the save gate navigates to the Print page without saving — same removed Print/Export button. The skip-without-saving path is covered by the same file's renamed test "authenticated + dirty: Skip on the save gate runs the export without saving", which asserts the download completes and zero save requests were made, in place of the old navigation to /print.
coverage-ack: frontend/tests/UnsavedWorkGuard.spec.ts::Unsaved-work guard (priority bug fix) > editor -> /print with cards selected does not show any dialog, and the deck's cards are the ones sent to print — the test reached /print via the removed Print/Export button; that navigation no longer exists, so there is nothing left to navigate to. The false-positive-guard regression this test protected against is covered by the same describe block's renamed test "editor: exporting a PDF with cards selected does not show any dialog, and the export completes", which asserts no beforeunload dialog fires and the download completes with the deck's card via the export flow that replaced the navigation.

# Editor export rescue, continued (PR #810 CI fix) - the same button/navigation removal above
# also broke a second cluster this file's own earlier ack pass missed: every test below drives
# PDFGenerator.tsx's dedicated wait-experience UI (the progress bar, the lazy-mounted QuestionFeed
# minigame embed) or the standalone /print route itself, reached ONLY by navigating there from a
# populated editor project. pages/print.tsx's own module comment states plainly that this page
# "now has no in-app entry point at all - only a direct/bookmarked URL reaches it, always with
# `isProjectEmpty` true" (a hard page.goto() is a real browser navigation, so it always loses the
# in-memory Redux store a populated project lives in) - confirmed independently: no
# router.push("/print")/<Link href="/print"> remains anywhere in src/. DisplayExportPDF.tsx (the
# new in-place export entry, pdfDownload.tsx's shared render pipeline) never mounts
# PDFWaitPanel.tsx - no progress bar, no game embed - so unlike the gate/save-prompt tests above
# (same Modal components, reachable through the new dropdown instead), this cluster's own subject
# UI has no reachable equivalent to port onto, in the app or in a test. Genuinely dropped, not
# renamed - deleting the four now-entirely-unreachable files (PDFGenerator.spec.ts,
# PagePreview.spec.ts, PostExportContributionPrompt.spec.ts, PDFWaitExperience.spec.ts) and this
# one test out of CardbackPdfWaitFidelity.spec.ts (its sibling E.1/E.2 test in the same file DOES
# stay - the reminder gate and apply-prompt Modals it covers are the same components, reached
# through the new Export-dropdown flow, same file's own updated test).
coverage-ack: frontend/tests/PDFGenerator.spec.ts::* — /print's own PDFGenerator.tsx (card-image-fetch-failure confirm, image-fetch progress, manual bleed override, bleed preview badge) has no reachable client path left once populated (see this section's header comment); dropped, not ported.
coverage-ack: frontend/tests/PagePreview.spec.ts::* — same unreachable /print route (fast DOM preview vs. pdf.js canvas render, both /print-only surfaces); dropped, not ported.
coverage-ack: frontend/tests/PostExportContributionPrompt.spec.ts::* — its one remaining test drove PDFGenerator.tsx's own /print mount, now unreachable; the prompt itself still gets exercised as part of the PDF-wait game embed's outro, which is also being dropped in this same pass (see PDFWaitExperience.spec.ts's own ack below) since that embed only ever mounted on /print too.
coverage-ack: frontend/tests/PDFWaitExperience.spec.ts::* — the progress-bar phases and the lazy-mounted QuestionFeed game embed are PDFGenerator.tsx-only UI (PDFWaitPanel.tsx), never wired into DisplayExportPDF.tsx's in-place export; no reachable path, dropped rather than ported.
coverage-ack: frontend/tests/CardbackPdfWaitFidelity.spec.ts::Cardback flow CSS fidelity (SPEC-cardback-pdfwait.md §E) - ${viewport.label} > E.3 progress bar + E.4 game embed tokens resolve real computed values at ${viewport.label} — same PDFWaitPanel.tsx tokens as the PDFWaitExperience.spec.ts ack above, reached the same now-unreachable way; this file's sibling E.1/E.2 test (same describe block) is fixed and kept, not dropped - it covers the reminder gate/apply-prompt Modals, which are reachable again through the new Export-dropdown flow.

# ChangeQueryModal precise-DFC-resolution round - the DFC-back-pair suggestion now resolves
# precisely unconditionally, regardless of the live Fuzzy/Precise search preference, because
# retyping a slot's query refines which printing of an already-bound card is wanted rather than
# discovering a new card. The old title's claim (DFC prompt appears for a prefix query under
# fuzzy search) is now false, not merely stale, so it was renamed and its assertion inverted in
# the same describe block rather than deleted outright.
coverage-ack: frontend/tests/ChangeQueryModal.spec.ts::ChangeQueryModal DFC pair tests > fuzzy search > DFC prompt shown for a prefix query when fuzzy search is enabled — asserted the DFC-back-pair suggestion appears for a prefix query when the fuzzy search preference is enabled; the suggestion now resolves precisely unconditionally regardless of the fuzzy/precise setting, so that claim is false rather than stale. Renamed to "DFC prompt not shown for a prefix query even when fuzzy search is enabled" in the same describe block, asserting the inverted (now-correct) outcome for the same prefix-query fixture - same surface, opposite result, not a coverage drop.

# Rail-anchored filters round (SelectVersionResults.tsx) - the Filters panel stopped rendering as a
# page-darkening backdrop + centered float dialog (ReactDOM.createPortal to document.body) at
# tablet/desktop widths; it is now ONE in-rail Collapse at every viewport tier (phone's existing
# .fpanel.inline presentation, generalized) - no portal, no scrim, drawn in place below the Select
# Version header. Both titles below named that retired desktop/tablet float+backdrop presentation
# explicitly; neither test was deleted - both are the SAME test, same describe block, same position
# in the file, with their bodies rewritten in place to assert the new in-rail mechanism instead.
coverage-ack: frontend/tests/DisplayLeftRailFidelity.spec.ts::Display left rail CSS fidelity guard (SPEC-rail-delegacy.md) > rail-head (rev #1/#2/#3), D14, Select Version header, and the desktop/tablet float Filters panel resolve the spec's literal §D values, not Bootstrap defaults — renamed to "rail-head (rev #1/#2/#3), D14, Select Version header, and the rail-anchored Filters panel resolve the spec's literal §D values, not Bootstrap defaults"; the assertions on filtersToggle opening a fixed-positioned 440px float panel with a scrim (closed via a backdrop click) were rewritten in place, same test, to assert the panel is the same in-rail `filters-panel-inline` node phone already used (not fixed-positioned, no `filters-panel-float`/`filters-panel-scrim` node mounted at all), with the candidate grid staying visible underneath and the panel closing via its own Close button instead of a backdrop click.
coverage-ack: frontend/tests/DisplayLeftRailFidelity.spec.ts::Display left rail CSS fidelity guard (SPEC-rail-delegacy.md) > at phone width (390px) the Filters panel expands IN PLACE inline - the desktop/tablet float node and its backdrop are never mounted — renamed to "at phone width (390px) the Filters panel expands IN PLACE inline - the retired float node and its backdrop are never mounted"; the assertion itself (opening Filters at 390px renders `filters-panel-inline` and never mounts `filters-panel-float`/`filters-panel-scrim`) is byte-identical - only the title's framing changed, from "desktop/tablet float node" (implying a float variant still exists at other viewports) to "retired float node" (accurate now that the float/scrim presentation this test compared phone against no longer exists at any viewport).

# Artist applet always-visible-credit round (issue #709 amendment) - the illustration-credit
# ArtistSupportLink's collapsed state changed from one line (page-link button only, credit
# gated behind the toggle) to two compact lines (page-link button, then the MTGAC credit line
# shown unconditionally) so attribution is visible without a click; only the commerce/badge
# panel still gates behind the toggle. The old title's "compact by default... its expansion"
# framing described the one-line/credit-behind-toggle shape that no longer exists, so it was
# renamed in the same change - the overlap-vs-pinned-reference-image guarantee itself (the
# boxesIntersect check against the pinned reference image) is unchanged and still asserted,
# just against the commerce/badge panel's expansion instead of the credit's.
coverage-ack: frontend/tests/QuestionFeed.spec.ts::question feed - Level 2 illustration grouping > the illustration-credit Artist Support Link is compact by default and its expansion never overlaps the pinned reference image — renamed to "the illustration-credit Artist Support Link shows its MTGAC credit without expanding, and its commerce/badge panel expansion never overlaps the pinned reference image" when the collapsed state changed to show the MTGAC credit unconditionally (attribution visible without a click); the overlap-vs-pinned-reference-image assertion is unchanged and still present, now scoped to the commerce/badge panel's expansion instead of the credit's.
# R9 cardback swatch-strip round (docs/proposals/mockups/editor-repass/SPEC-editor-repass.md
# item 2, 2026-08-15) - the per-slot (left rail) picker and the right rail's toolbar entry are
# now BOTH the shared CardbackSwatchStrip primitive (CardbackRailControl/CommonCardback.tsx,
# SlotCardbackControl.tsx). The old CardbackToolbarButton trigger is retired outright (the full
# GridSelectorModal is still reachable via "Browse all cardbacks…", testid
# cardback-browse-all-button), and the per-slot surface's never-pre-checked trap-guard prompt is
# retired WITH the per-slot apply/set-default affordances R9 deletes. Both titles below are
# renamed (not dropped) - the still-covered guarantees they carried are preserved under the new
# titles, asserted against the strip/rail mechanisms instead of the retired ones.
coverage-ack: frontend/tests/CardbackFlow.spec.ts::Cardback apply-all + set-default prompt (SPEC-cardback-pdfwait.md §C.2, PKG1b) > rail (per-slot) entry stays per-slot with a never-pre-checked trap-guard, and the sheet's flip icon flags the resulting custom back; the toolbar (project-wide) entry then shows the affected slot's thumbnails + count, and Apply to all/Set default both work independently — R9: the old title's single test is SPLIT into two tests under the new describe "Cardback apply-all + set-default (SPEC-cardback-pdfwait.md §C.2, PKG1b; R9 strip round)". "rail (per-slot) entry picks per-slot via the shared swatch strip - no apply/set-default affordances and no trap-guard prompt any more - and the sheet's flip icon flags the resulting custom back" keeps the per-slot-stays-per-slot + flip-icon guarantees, but the never-pre-checked trap-guard half is retired with the per-slot prompt R9 deletes (no per-slot apply/set-default exists to guard against any more). "toolbar (project-wide) entry: the right rail's swatch strip + its two plain buttons apply and remember independently, and Browse all cardbacks… still opens the full modal with the unchanged apply prompt" carries the old toolbar half's guarantees forward unchanged (project-wide pick, affected thumbnails + count, Apply to all/Set default done states) now reached via the rail strip + Browse-all modal instead of the old button. Plus a NEW third test (gate swatch-strip pick, describe §C.1) - added coverage, no ack needed for additions.
coverage-ack: frontend/tests/DisplayPage.spec.ts::DisplayPage (Proposal H, Step 1) > the Cardback toolbar button opens the same project-wide cardback picker the classic editor uses, and a selection made through it updates back-face slots on the sheet — R9: CardbackToolbarButton (testid cardback-toolbar-button) is retired in favour of CardbackRailControl's "Browse all cardbacks…" button (testid cardback-browse-all-button, same MemoizedCommonCardbackGridSelector/GridSelectorModal verbatim). Renamed to "the right rail's Browse all cardbacks… opens the same project-wide cardback picker the classic editor uses, and a selection made through it updates back-face slots on the sheet" - identical guarantees (picker opens from the right rail, selection round-trips through shared projectSlice.cardback state onto a back-face slot), extended with the R9 strip-section presence assertions (cardback-rail-control/cardback-rail-strip).
coverage-ack: frontend/tests/ContrastAudit.spec.ts::Contrast audit - editor mobile Print & Settings sheet (owner defect 3) > Showing: Fronts / Cardback / Export controls in the offcanvas — renamed to "View segments / Cardback / Export controls in the offcanvas" by editor-repass R10.2: the right-rail View control became a Fronts|Backs ToggleButtonGroup, so the "Showing: Fronts" label it asserted no longer exists as page text; the contrast audit itself is unchanged and still runs against the same three controls.

# Print cut-guide redesign (fix/print-cut-guides) - the CutLinePlacement (Inside/Centre/Outside)
# and CutLineShape (Cross/InsideOnly/OutsideOnly/Dashed) option model is replaced by a single
# always-on dashed trim outline plus an independent, default-off crosshair-marks toggle
# (PDFProps.showCrossCutLines) - see PDF.tsx's own module comments. Neither removed field was
# ever persisted (not in deckPayload.ts, not in localStorage, not redux-persisted - confirmed by
# search before this change), so there is no saved-settings migration concern, only the two
# Playwright titles below that named the retired shape/placement selects directly.
coverage-ack: frontend/tests/DisplayExportPDFSettings.spec.ts::DisplayExportPDF - editor export controls > cut-line colour and shape controls are only shown when the rail's Guides toggle is on, and map through to the export — renamed to "cut-line colour and cross-marks toggle are only shown when the rail's Guides toggle is on, and map through to the export": the cut-line-shape select is retired (CutLineShape deleted, PDF.tsx now always draws a dashed trim outline), replaced by the display-export-cross-cut-lines toggle (PDFProps.showCrossCutLines); the guides-on/off gating assertion is unchanged, now exercised against the new toggle.
coverage-ack: frontend/tests/DisplayExportPDFSettings.spec.ts::DisplayExportPDF - corner rounding and extended cut-line geometry > placement, length, thickness, and offset are settable alongside colour and shape — renamed to "length, thickness, and offset are settable alongside colour": the cut-line-placement select is retired (CutLinePlacement deleted - the trim outline has one correct position, on the trim boundary, not a user choice); the length/thickness/offset assertions are unchanged and kept.

# Export-settings-to-rail round - image DPI, JPG quality, and corner rounding moved out of
# DisplayExportPDF.tsx's own settings step into the right rail's new "Print quality" section
# (they govern the printed card's own appearance, not a one-off export-run choice like a page
# range or cut-line geometry), so their testids and the one test that drives them by testid
# changed with the move. Same signal asserted (the actual dpi/jpgQuality query params on the
# downstream image-worker request), now set via the rail's display-image-dpi/display-jpg-quality
# controls before opening the export dialog instead of via the dialog's own (now-removed)
# display-export-image-dpi/display-export-jpg-quality controls.
coverage-ack: frontend/tests/DisplayExportPDFSettings.spec.ts::DisplayExportPDF - editor export controls > DPI and JPG quality sliders set the actual image-worker request's dpi/jpgQuality query params — renamed to "the rail's DPI and JPG quality controls set the actual image-worker request's dpi/jpgQuality query params" when the two controls moved from the export dialog into the right rail's "Print quality" section; same dpi/jpgQuality query-param assertion, now driven through the rail's own testids ahead of opening the export dialog (the dialog's backdrop blocks the rail behind it while open).

# Card-selection-and-page-range-to-rail round - "Cards to include" and the page range control
# moved out of DisplayExportPDF.tsx's own settings step into the right rail's new "Export"
# section (they decide which cards make it into the export and which of its pages actually
# render - properties of the artifact, not a one-off export-run choice, same reasoning as the
# DPI/JPG/corner-rounding and cut-line-appearance rounds before it). The renamed test's own
# assertions are unchanged (default selection mode value, real non-guessed total page count),
# now read straight off the rail with no dialog to open first; every other test in this file
# that exercises either control kept its title and had only its body's testids/ordering updated
# in place (fill the rail control before opening the dialog, since its backdrop blocks the rail
# behind it while open - same constraint the DPI/JPG round above already documents).
coverage-ack: frontend/tests/DisplayExportPDFSettings.spec.ts::DisplayExportPDF - editor export controls > opens with a safe default selection mode and a real, non-guessed total page count — renamed to "the rail defaults to a safe selection mode and shows a real, non-guessed total page count" when "Cards to include" and the page range control moved from the export dialog into the right rail's new "Export" section; same assertions (default selection mode value, real non-guessed total page count), now read directly off the rail with no dialog to open first.

# Page-cut-guide-lines-to-rail round - the page cut lines toggle moved out of
# DisplayExportPDF.tsx's own settings step into the right rail's "Cut lines & snip guides"
# section (same "properties of the artifact, not a one-off export-run choice" reasoning as the
# DPI/JPG/corner-rounding and card-selection/page-range rounds above it). The describe block and
# test title were renamed off the settings-step control's testid
# (display-export-page-cut-lines) and off the stale "(rescued from /print's PDFGenerator.tsx)"
# framing; the test body's real-PDF assertion is byte-identical - countPageOneDrawOps still
# parses the downloaded PDF's actual operator list and asserts onOps > offOps - now set via the
# rail's display-page-cut-lines-toggle (behind expandRailSection(page, "cut-lines-guides")) ahead
# of opening the export dialog, same backdrop-blocks-the-rail constraint the DPI/JPG round above
# documents.
coverage-ack: frontend/tests/DisplayExportPDFSettings.spec.ts::DisplayExportPDF - page cut guide lines (rescued from /print's PDFGenerator.tsx) > drawPageCutLines reaches the rendered PDF: turning the toggle off removes real draw operations, not just UI state — renamed to "drawPageCutLines reaches the rendered PDF: turning the rail toggle off removes real draw operations, not just UI state" (describe renamed to "DisplayExportPDF - page cut guide lines (rail)") when the toggle moved from the export dialog's display-export-page-cut-lines control into the right rail's display-page-cut-lines-toggle; the countPageOneDrawOps/onOps>offOps real-draw-op assertion is byte-identical, only the control's location and testid changed.
