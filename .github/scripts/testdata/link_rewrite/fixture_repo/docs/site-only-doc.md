# Site Only Doc

Fixture source document (site-only target, deliberately no "wiki" key in
map.json — exercises publish_wiki.py's build_repo_to_wiki_map/main()
skipping a page with no wiki target instead of KeyError-ing).
