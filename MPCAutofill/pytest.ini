[pytest]
DJANGO_SETTINGS_MODULE = MPCAutofill.settings
markers =
    real_host_load: opt out of conftest's `deterministic_host_load` fixture and sample the real `os.getloadavg()`.
    real_process_rss: opt out of conftest's `deterministic_process_rss` fixture and sample this process's real RSS.
